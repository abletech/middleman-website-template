<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
	<% data.menu.each do |m| %>
		<% if m[:sitemap] %>
		<url>
			<loc><%= url_root %><%= m[:url] %></loc>
			<priority><%= m[:priority] %></priority>
		</url>
		<% end %>
	<% end %>
</urlset>
